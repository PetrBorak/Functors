import { expect } from 'chai';
import { Map } from './index'

describe('helpers/Map', function() {
  it('callabsle shsould be imported', function() {
    return expect(typeof Map, 'function was imported').to.be.equal('function');
  });
});
