import { traverse } from './traverse';
import { expect } from 'chai'

describe('helpers/poitnfree utilities/add', () =>{
  it('import function', () => {
    return expect(typeof traverse, 'is of type function').to.be.equal('function')
  })
})
