import { expect } from 'chai';
import { Either } from './index'

describe('helpers/Either', function() {
  it('callabsle shsould be imported', function() {
    return expect(typeof Either, 'function was imported').to.be.equal('function');
  });
});
